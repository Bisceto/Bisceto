
function square(x) {
    return x * x;
}
/*
function repeat_pattern(n, pat, init) {
  return n === 0
         ? init 
         : pat(repeat_pattern(n - 1, pat, init));
}

repeat_pattern(3, square, 2);
*/
function repeat_pattern1(n, pat, init) {
  return n === 0 
         ? init
         : repeat_pattern1(n - 1, pat, pat(init));
}

repeat_pattern1(3, square , 2);